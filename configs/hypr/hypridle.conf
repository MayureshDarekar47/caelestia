general {
    lock_cmd = hyprlock
    before_sleep_cmd = hyprlock
}

listener {
    timeout = 240
    on-timeout = loginctl lock-session
}

listener {
    timeout = 1800
    on-timeout = systemctl suspend
}

#general {
 #   lock_cmd = hyprlock
  #  before_sleep_cmd = hyprlock
#}

#listener {
 #   timeout = 240
  #  on-timeout = loginctl lock-session
#}

#listener {
 #   timeout = 1800
  #  on-timeout = systemctl suspend
#}



